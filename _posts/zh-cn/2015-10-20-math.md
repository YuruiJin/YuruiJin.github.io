---
layout: post
title: 带有数学公式的文章
date: 2015-10-20 11:12:00-0400
description: 带有数学公式的博客文章示例
tags: formatting math
categories: sample-posts
related_posts: false
---

本主题支持使用 [MathJax 3](https://www.mathjax.org/) 引擎在内联和显示模式下渲染精美的数学公式。您只需用 `$$` 包围数学表达式即可，如 `$$ E = mc^2 $$`。如果将其放在段落中，将产生内联表达式，就像 $$ E = mc^2 $$。

要使用显示模式，同样用 `$$` 包围表达式并将其作为单独的段落。以下是一个示例：

$$
\sum_{k=1}^\infty |\langle x, e_k \rangle|^2 \leq \|x\|^2
$$

您也可以使用 `\begin{equation}...\end{equation}` 代替 `$$` 来实现显示模式的数学公式。
MathJax 将自动为方程编号：

\begin{equation}
\label{eq:cauchy-schwarz}
\left( \sum_{k=1}^n a_k b_k \right)^2 \leq \left( \sum_{k=1}^n a_k^2 \right) \left( \sum_{k=1}^n b_k^2 \right)
\end{equation}

通过在方程环境中添加 `\label{...}`，我们现在可以使用 `\eqref` 引用该方程。

请注意，MathJax 3 是 [MathJax 的重大重写](https://docs.mathjax.org/en/latest/upgrading/whats-new-3.0.html)，大幅提升了加载和渲染速度，目前[与 KaTeX 相当](http://www.intmath.com/cg5/katex-mathjax-comparison.php)。
