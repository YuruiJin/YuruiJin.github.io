<br>
<hr>
<br>

{{ site.data[site.active_lang].strings.citation.introduction }}

{% assign localized_first_name = site.data[site.active_lang].strings.first_name | default: site.first_name %}
{% assign localized_middle_name = site.data[site.active_lang].strings.middle_name | default: site.middle_name %}
{% assign localized_last_name = site.data[site.active_lang].strings.last_name | default: site.last_name %}

{% capture citation_quote -%}
> {{ localized_last_name }}, {{ localized_first_name }}{% if localized_middle_name %} {{ localized_middle_name }}{% endif %} ({{ page.date | date: "%b %Y" }}). {{ page.title }}. {% if site.title != 'blank' %}{{ site.title }}. {% endif %}{{ site.url }}.
{%- endcapture %}
{{ citation_quote | markdownify }}

<p>{{ site.data[site.active_lang].strings.citation.bibtex }}</p>

{% capture citation_code -%}
```bibtex
@article{ {{- localized_last_name | downcase }}{{ page.date | date: "%Y" }}{{ page.title | slugify }},
  title   = { {{- page.title -}} },
  author  = { {{- localized_last_name }}, {{ localized_first_name }}{% if localized_middle_name %} {{ localized_middle_name }}{% endif -%}},
  {%- if site.title != 'blank' %}journal = { {{- site.title -}} },{% endif %}
  year    = { {{- page.date | date: "%Y" -}} },
  month   = { {{- page.date | date: "%b" -}} },
  url     = { {{- site.url }}{{ page.url -}} }
}
```
{%- endcapture %}
{{ citation_code | markdownify }}
